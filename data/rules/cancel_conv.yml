version: "3.1"

rules:

  - rule: conversation cancel
    steps:
      - intent: cancel_conversation
      - action: action_fallback

  - rule: conversation cancel
    steps:
      - intent: cancel_conversation_ar
      - action: action_fallback